import 'package:flutter/material.dart';

class PracticeFeature extends StatelessWidget {
  const PracticeFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
