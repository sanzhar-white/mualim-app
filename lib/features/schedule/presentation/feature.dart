import 'package:flutter/material.dart';

class ScheduleFeature extends StatelessWidget {
  const ScheduleFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
