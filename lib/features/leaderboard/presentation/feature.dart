import 'package:flutter/material.dart';

class LeaderboardFeature extends StatelessWidget {
  const LeaderboardFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
